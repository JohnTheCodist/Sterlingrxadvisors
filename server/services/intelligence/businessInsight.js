/**
 * Signal Fusion Engine — type definition only. No logic, no I/O.
 *
 * @typedef {Object} BusinessInsight
 * @property {string} category
 * @property {"Increase"|"Decrease"|"Neutral"} overallDemand
 * @property {number} confidence - 0.0-1.0
 * @property {string[]} supportingSources - Distinct source names backing overallDemand.
 * @property {string} rationale
 */

module.exports = {};
